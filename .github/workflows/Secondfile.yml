name: Second_CICD
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
        
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
      - name: Ran third multi line command
        run: |
          echo "Mohan"
          echo "Shyam"
  
  Test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test the code
        run: echo "Running test code"
  
  deploy:
    runs-on: ubuntu-latest
    needs: Test
    steps:
       - name: deploy the app
         run: echo "deploying the app"
